# Track: Stadium

Long straights, four sweeping corners. Schema v2 with grass runoff (outside 90 / inside 50) and a single gravel patch on the top-right corner.

## Completed
- Procedural centerline (~80 points) from `scripts/gen-tracks.mjs`.
- v2 JSON with runoff config and the corner gravel patch (annular sector).
- `startIndex = 4` so the grid lines up on the mid-top straight.
- `MenuScene.TRACKS` entry; selectable from menu.

## Open Questions
- None track-specific.

## Next Up
- None planned.
